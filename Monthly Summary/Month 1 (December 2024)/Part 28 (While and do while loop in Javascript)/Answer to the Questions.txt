1. This is the syntax of a while loop:
    let index = 10;
    while (index < 10) {
        console.log(index);
        index = index + 2;
    }

2. Like this:
    let myArr = ['Masud', 'Ataullah', 'Muhammad']
    while(index < myArr.length) {
    console.log(myArr[index]);
    index = index + 1;
    }

3. syntax of do while loop:
    let score = 1;
    do {
    console.log(score);
    score++;
    } while (score <= 10);

    work of do while loop:
    This is works first and checks the condition last, if the condition was true it will work second time and so far;