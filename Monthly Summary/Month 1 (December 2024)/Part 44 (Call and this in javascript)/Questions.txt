1. When we call a function in another function, then what is the context of the inner function that you called from an outside function?

2. what referes the Global Execution Context in node and browser?

3. When we call a function in another function, then is really calls that outer side function?

4. How to solve the problem, when we face this type of problem (variables vanishing from functions)?

5. When we use the 'call' method?

6. And there is also a problem, and that is: the outer side function is using his 'this', but that will vanish after call the function, so how to do this: the outer function uses the main function's 'this'?

7. try the whole tutorial.