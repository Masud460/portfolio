1. Transition make change when an object change a shape to another shape;

2. This is a good practice to decress the transition duration;

3. The transition-property defines in what property the transition will apply, let's a if you set the transition-property to 'background' it only transit in the background, and if you set it to 'width' it will transit only when the width changes, not in the background, because the background will change instantly.

4. This is with comma separeted;

5. These are the propertys: ease, ease-in(starts slowly and end fast), ease-out(starts fast and end slowly), ease-in-out(starts slowly then fast and end slowly);